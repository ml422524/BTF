#ifndef _VARIABLE_H_
#define _VARIABLE_H_

#include <string>

using std::string;

//存储(即，写)资源路径
extern string reswpath;
extern string restfwpath;
extern string respicwpath;
//读取资源路径
extern string resrpath;
extern string restfrpath;
extern string respicrpath;

#endif
